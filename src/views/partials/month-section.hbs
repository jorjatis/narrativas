{{#each months}}
  <section class="month-group">
    <div class="month-group__header">
      <h2 class="month-title">{{title}}</h2>
    </div>

    <div class="grid">
      {{#each cards}}
        {{> ./card }}
      {{/each}}
    </div>

  </section>
{{/each}}
