// import { gsap, ScrollTrigger } from './vendors/gsap.min.js'; // Si lo necesito

export default function moduleMockup() {
  // const mockUp = document.querySelector(".claseMockUp");

  // if (!mockUp) return;
  // if (!mockUp || !gsap || !ScrollTrigger) return; // Si hay GSAP

  // codigo aqui
}

